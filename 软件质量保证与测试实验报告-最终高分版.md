课程编号：A0801050230

# 软件质量保证与测试
# 实验报告

| 组长姓名 | 请填写组长姓名 | 学 号 | 请填写组长学号 |
| --- | --- | --- | --- |
| 班 级 | 请填写班级 | 指 导 教 师 | 吴辰铌 |
| 开 设 学 期 | 2025-2026 春季学期 |  |  |
| 开 设 时 间 | 第15周--第16周 |  |  |
| 报告日期 | 请填写提交日期 |  |  |
| 评 定 成 绩 |  | 评定人 | 吴辰铌 |
|  |  | 评 定 日 期 | 2026.6.19 |

东北大学软件学院

## 小组成员与任务分工

本课程实验说明要求分组人数不超过 4 人。本报告按 4 人小组组织，实验材料由组长统一提交。

| 成员 | 负责内容 | 主要产出 |
| --- | --- | --- |
| 成员A | 总体测试计划、AI 问答、AI 草稿、风险边界 | 测试范围、AI 模块用例、草稿状态迁移、风险分析 |
| 成员B | 任务待办、日程提醒、控制台交互 | 任务/日程单元测试、边界值、状态迁移、CLI 黑盒用例 |
| 成员C | 学习计划、收支记录、笔记管理 | 学习/财务/笔记测试用例、统计路径、异常输入分析 |
| 成员D | 汇总统计、自动化测试、CI、覆盖率、缺陷回归 | 汇总链路、Maven/JaCoCo/CI 证据、缺陷闭环、报告统稿 |

## 1. 实验目的

（1）练习白盒测试用例设计，围绕 `java-ai-assistant` 项目的服务层、值对象、查询对象、AI 调用封装和状态迁移逻辑，使用 JUnit Jupiter、Mockito、Maven Surefire 和 JaCoCo 进行单元测试与覆盖证据整理。

（2）练习黑盒测试用例设计，从用户视角对控制台个人学习生活助手进行系统测试和验收测试，验证任务、日程、学习计划、收支记录、笔记、汇总统计、AI 问答和 AI 草稿确认导入等核心工作流。

（3）掌握软件质量保证过程中的测试计划、测试策略、测试用例设计、自动化测试、CI、缺陷记录、回归测试和质量度量方法，形成可复现、可追踪、可评分的实验材料。

## 2. 测试计划

### 2.1 实验1测试计划

#### （1）系统功能概述

本组待测程序为 `java-ai-assistant`。该系统是一个基于 Java 17 和 Maven 的控制台个人学习生活助手，运行入口为 `assistant.app.Main`。系统面向大学生日常学习和生活管理，包含 8 个核心功能：AI 问答、AI 结构化建议确认导入、任务待办、日程提醒、学习计划、收支记录、个人笔记、数据查询与汇总统计。功能数量为 8，大于小组人数，满足实验指导书“功能数要不少于本组人数”的要求。

当前版本采用内存仓储，不包含数据库持久化、图形界面、多用户账号、系统通知和真实后台提醒。普通单元测试不访问真实 DeepSeek、真实网络、真实 API Key、真实用户文件或真实当前时间。

| 功能模块 | 说明 | 代表性生产类 |
| --- | --- | --- |
| AI 问答与学习生活建议 | 根据本地上下文构造 prompt，并封装 DeepSeek 兼容请求 | `AiAssistantService`, `PromptBuilder`, `DeepSeekAiClient` |
| AI 结构化建议确认导入 | 解析任务或学习计划草稿，支持确认、取消、导入失败回滚 | `StructuredSuggestionDraftService`, `DraftLifecycleService`, `DraftImportService` |
| 任务待办管理 | 新增、查询、筛选、完成、撤销、删除任务 | `TaskService`, `TaskItem`, `TaskQuery` |
| 日程提醒管理 | 新增日程、按日期查询、冲突检测、状态计算 | `ScheduleService`, `ScheduleConflictPolicy` |
| 学习计划管理 | 创建计划、更新进度、状态推导和统计 | `StudyPlanService`, `StudyPlanAnalysisService` |
| 收支记录管理 | 记录收入支出、筛选、统计收入支出结余 | `FinanceService`, `FinanceStatisticsService` |
| 笔记或日记管理 | 新增、修改、删除、关键字搜索、标签搜索 | `NoteService`, `NoteSearchPolicy` |
| 数据查询与汇总统计 | 汇总任务、日程、学习计划、收支、笔记并生成 AI 本地上下文 | `SummaryService`, `DashboardSummary`, `LocalContext` |

#### （2）测试目标

实验 1 以白盒测试为主，目标是验证 8 个核心功能的业务正确性、异常输入处理、状态迁移、跨模块汇总和 AI 外部依赖隔离。测试应覆盖语句、判定、条件、基本路径、边界值、等价类、错误推测和状态迁移，并通过 JUnit 运行结果、JaCoCo 覆盖证据和缺陷回归记录形成闭环。

#### （3）测试范围

| 范围类型 | 内容 |
| --- | --- |
| 被测模块 | AI、AI 草稿、任务、日程、学习计划、收支、笔记、汇总、控制台入口、文档与 CI |
| 被测层级 | 服务层、实体和值对象、查询对象、策略对象、AI 客户端封装、控制台输入解析 |
| 不测范围 | 真实 DeepSeek 网络连通性、真实 API Key、数据库持久化、GUI、移动端、多用户并发、系统通知 |
| 可选扩展 | `mvn -Pintegration verify` 作为未来真实 DeepSeek `*IT.java` 集成测试入口 |

#### （4）测试环境

| 环境项 | 版本或说明 |
| --- | --- |
| JDK | Java 17 |
| 构建工具 | Maven |
| 单元测试框架 | JUnit Jupiter 5.14.4 |
| Mock 工具 | Mockito 5.18.0 |
| 单元测试执行 | Maven Surefire Plugin 3.5.6 |
| 集成测试入口 | Maven Failsafe Plugin 3.5.6 |
| 覆盖率工具 | JaCoCo Maven Plugin 0.8.13 |
| 项目目录 | `java-ai-assistant` |

#### （5）测试工具

| 工具 | 用途 |
| --- | --- |
| JUnit Jupiter | 编写服务层、值对象、策略、AI 模块和控制台交互单元测试 |
| Mockito | 替换部分协作者，验证外部服务调用边界 |
| Maven Surefire | 执行默认单元测试，匹配普通 `*Test.java` |
| Maven Failsafe | 保留集成测试入口，匹配未来 `*IT.java` |
| JaCoCo | 生成覆盖率报告 |
| GitHub Actions | 在 push 和 pull_request 时执行构建与测试门禁 |

#### （6）测试策略

| 测试策略 | 项目化说明 |
| --- | --- |
| 语句覆盖 | 触达服务公开方法的成功路径和主要错误路径 |
| 判定覆盖 | 验证存在/不存在、合法/非法、启用/未启用、冲突/不冲突等分支 |
| 条件覆盖 | 构造状态、优先级、日期范围、类型、标签、AI HTTP 状态族等组合输入 |
| 基本路径测试 | 分析 `FinanceStatisticsService.calculate`、`DraftImportService.importDraft`、`SummaryService.getDashboardSummary` 等复杂方法独立路径 |
| 边界值 | 覆盖进度 0/100/-1/101、金额 0/负数/两位小数、首尾相接日程、空集合统计 |
| 等价类 | 将任务优先级、日程状态、学习计划状态、交易类型、笔记标签、AI 场景划分为有效/无效等价类 |
| 错误推测 | 针对空标题、缺失 dueDate、格式异常 JSON、HTTP 401/429/5xx、超时、不存在 id 构造测试 |
| 状态迁移 | 验证任务完成/撤销、草稿确认/取消/重复确认、学习计划进度更新后的状态变化 |
| 回归测试 | 缺陷修复后重跑关联测试类和全量 `mvn clean verify` |

### 2.2 实验2测试计划

#### （1）测试目标

实验 2 以黑盒测试、系统测试和验收测试为主，目标是从用户视角验证控制台菜单能完成核心工作流，并验证非法输入、无效编号、空字段、日期格式错误、冲突日程、未配置 AI 等异常场景能给出稳定反馈。

#### （2）测试范围

| 范围类型 | 内容 |
| --- | --- |
| 黑盒主流程 | 启动退出、汇总查看、任务增删改查、日程冲突、学习计划进度、收支统计、笔记搜索、AI 未配置提示、草稿确认/取消 |
| 系统边界 | 控制台输入输出、菜单循环、错误提示、跨模块摘要同步 |
| 自动化范围 | `mvn clean test`、`mvn clean verify`、`mvn jacoco:report`、`mvn -Pintegration verify`、GitHub Actions CI |
| 不测范围 | 真实 DeepSeek 网络连通性、真实 API Key、数据库持久化、多用户并发、GUI |

#### （3）测试环境

实验 2 使用与实验 1 相同的 Java 17 和 Maven 环境。黑盒测试以控制台程序为对象，运行前在 `java-ai-assistant` 目录执行：

```bash
mvn -q -DskipTests dependency:build-classpath -Dmdep.outputFile=target/classpath.txt
mvn -q -DskipTests compile
java -cp "target/classes:$(cat target/classpath.txt)" assistant.app.Main
```

Windows PowerShell 环境使用以下命令运行：

```powershell
mvn -q -DskipTests dependency:build-classpath -Dmdep.outputFile=target/classpath.txt
mvn -q -DskipTests compile
$cp = "target/classes;" + (Get-Content target/classpath.txt)
java -cp $cp assistant.app.Main
```

#### （4）测试工具

控制台手工输入用于验证用户菜单、输入解析和提示信息；`ConsoleApplicationTest` 用于自动化覆盖菜单循环和异常输入；Maven 用于执行构建、测试和验证；JaCoCo 提供覆盖率证据；GitHub Actions 提供 CI 自动化验证。

#### （5）测试策略

实验 2 按正常场景、异常场景、边界场景、回归场景和自动化场景组织。正常场景按用户真实操作流程输入数据并验证输出；异常场景输入空标题、非法日期、非法金额、无效编号和冲突时间；边界场景验证进度 0 和 100、金额两位小数、日程首尾相接、空集合统计；回归场景在缺陷修复后重跑关联测试类和全量测试。

## 3.测试用例

### 3.1 实验1测试用例及结果分析

#### （1）测试用例

实验 1 代表性白盒用例如下，完整用例见 `java-ai-assistant/docs/test-cases.md`。

| 编号 | 测试方法 | 被测类/方法 | JUnit 测试类/方法 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| AI-01 | 基本路径 | `AiAssistantService.ask` | `AiAssistantServiceTest.askSendsBuiltRequestToClientAndReturnsContent` | fake 本地上下文、fake AI 返回内容 | 返回成功响应内容 | 通过 |
| AI-02 | 判定覆盖 | `AiAssistantService.ask` | `AiAssistantServiceTest.askReturnsNotConfiguredWithoutCallingCollaborators` | AI 配置未启用 | 返回未配置失败且不调用协作者 | 通过 |
| AI-06 | 错误推测 | `DeepSeekAiClient.chat` | `DeepSeekAiClientTest.chatMapsEmptyResponseShapes` | 空 choices、缺失 message、非 JSON 响应 | 映射为空响应或格式错误 | 通过 |
| DRAFT-04 | 状态迁移 | `DraftLifecycleService.confirmDraft` | `DraftLifecycleServiceTest.confirmDraftImportsAndMarksDraftImported` | 可确认草稿、导入服务成功 | 草稿状态变为已导入 | 通过 |
| DRAFT-08 | 回滚路径 | `DraftImportService.importDraft` | `DraftImportServiceTest.rollsBackCreatedTasksWhenTaskCreationFails` | 第二个任务创建失败 | 回滚已写入任务 | 通过 |
| TASK-01 | 基本路径 | `TaskService.createTask` | `TaskServiceTest.createTaskStoresTodoTaskAndReturnsTaskView` | 标题、描述、优先级、dueDate 有效 | 创建未完成任务 | 通过 |
| TASK-06 | 状态迁移 | `TaskService.markTaskCompleted`, `TaskService.reopenTask` | `TaskServiceTest.markTaskCompletedRejectsAlreadyCompletedTaskAndKeepsState` | 已完成任务重复完成 | 返回状态冲突，状态不变 | 通过 |
| SCHEDULE-03 | 判定覆盖 | `ScheduleConflictPolicy` | `ScheduleServiceTest.createScheduleRejectsOverlappingScheduleAndKeepsRepositoryUnchanged` | 已有日程与新日程重叠 | 拒绝创建 | 通过 |
| SCHEDULE-04 | 边界值 | `ScheduleConflictPolicy` | `ScheduleServiceTest.createScheduleAllowsTouchingTimeRanges` | 前一日程结束等于后一日程开始 | 不视为冲突 | 通过 |
| STUDY-04 | 边界值 | `StudyPlanService.createStudyPlan` | `StudyPlanServiceTest.createStudyPlanRejectsNegativeInitialProgressAndKeepsRepositoryUnchanged` | 初始进度 -1 或 101 | 返回校验失败 | 通过 |
| FINANCE-06 | 基本路径 | `FinanceStatisticsService.calculate` | `FinanceStatisticsServiceTest.calculateAccumulatesIncomeAndExpenseSeparately` | 多笔收入支出 | 分别累计并计算结余 | 通过 |
| NOTE-05 | 边界值 | `NoteService.searchByKeyword` | `NoteServiceTest.searchByKeywordRejectsNullOrBlankKeyword` | 空关键字 | 返回校验失败 | 通过 |
| SUMMARY-07 | 条件覆盖 | `SummaryService.getDashboardSummary` | `SummaryServiceTest.getDashboardSummaryQueriesServicesWithExpectedDateBoundaries` | 今日、逾期、未来高优先级任务混合 | 摘要按规则过滤 | 通过 |
| DOC-01 | 文档交付 | 指导书、测试计划、测试用例 | `DocumentationDeliveryTest` | 关键 Markdown 文件存在 | 文档路径和关键内容可验证 | 通过 |
| CI-01 | CI 交付 | `.github/workflows/ci.yml` | `CiWorkflowDeliveryTest` | push 或 pull_request | 执行构建和测试 | 通过 |

#### （2）结果分析

当前项目 `src/test/java/assistant` 下共有 79 个测试类，覆盖 AI、草稿、任务、日程、学习计划、收支、笔记、汇总、应用入口、公共值对象、可测试性支持、文档和 CI 等模块。2026-07-09 本地执行 `mvn -q clean verify` 通过，Surefire 汇总为 992 个测试、失败 0、错误 0、跳过 0。当前 JaCoCo 汇总为指令覆盖 96.78%、分支覆盖 86.61%、行覆盖 94.48%。课程现场复核应以最终提交前重新执行的 Maven、Surefire 和 JaCoCo 输出为准。

测试方法覆盖较充分：AI 模块覆盖未配置、空响应、格式异常、HTTP 错误和网络异常；任务模块覆盖创建、筛选、完成、重复完成、撤销和删除不存在；日程模块覆盖冲突判定和首尾相接边界；学习计划模块覆盖进度边界和日期范围；收支模块覆盖金额边界和统计路径；汇总模块覆盖今日、逾期、未来 7 天高优先级任务和多模块聚合。缺陷修复后均通过关联测试和全量回归验证。

### 3.2 实验2测试用例及结果分析

#### （1）测试用例

完整黑盒用例见 `java-ai-assistant/docs/black-box-test-cases.md`。代表性用例如下：

| 编号 | 场景 | 输入或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-START-01 | 启动并退出 | 启动程序，输入 `q` | 显示主菜单并提示已退出 | 通过 |
| BB-MENU-01 | 非法主菜单命令 | 输入 `x` | 提示未知命令并显示帮助 | 通过 |
| BB-SUMMARY-01 | 查看汇总 | 输入 `1` | 显示今日、任务、日程、学习计划、收支、笔记统计 | 通过 |
| BB-TASK-01 | 新增任务 | 菜单 `2` -> `a`，输入有效任务字段 | 显示任务详情，列表可见新任务 | 通过 |
| BB-TASK-02 | 任务空标题 | 新增任务时标题为空白 | 返回校验失败，任务列表不增加 | 通过 |
| BB-TASK-03 | 完成和撤销任务 | 对同一任务执行完成后撤销 | 状态从未完成变为已完成，再回到未完成 | 通过 |
| BB-SCHEDULE-02 | 冲突日程 | 新增与已有日程重叠的日程 | 返回 `SCHEDULE_CONFLICT`，冲突日程不写入 | 通过 |
| BB-STUDY-03 | 进度边界 | 输入 0、100、-1、101 | 0 和 100 接受，-1 和 101 拒绝 | 通过 |
| BB-FINANCE-03 | 非法金额 | 输入 0 或负数 | 返回金额必须为正数的校验错误 | 通过 |
| BB-NOTE-02 | 关键字搜索 | 输入存在于标题或内容的关键字 | 返回匹配笔记 | 通过 |
| BB-AI-01 | 未配置 AI 问答 | 菜单 `7`，输入问题 | 返回 `AI_NOT_CONFIGURED`，程序继续运行 | 通过 |
| BB-DRAFT-02 | 确认草稿 | 对可确认草稿执行确认 | 草稿变为已导入，任务或学习计划落地 | 通过 |
| BB-CONSOLE-01 | 中文枚举输入 | 输入 `高`、`已完成`、`收入`、`进行中` 等中文枚举 | 正确解析并以中文展示 | 通过 |

手工测试记录示例：

| 用例编号 | 执行步骤摘要 | 预期输出 | 实际输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| BB-START-01 | 启动程序后输入 `q` | 显示主菜单并退出 | 输出 `Java AI Assistant`、主菜单和“已退出。” | 通过 |
| BB-TASK-01 | 进入任务菜单，新增“今日任务” | 创建成功并显示任务详情 | 输出任务 ID、标题、优先级、状态、截止日期和描述 | 通过 |
| BB-SCHEDULE-02 | 创建重叠日程 | 拒绝冲突日程 | 输出 `SCHEDULE_CONFLICT`，列表中无冲突日程 | 通过 |
| BB-AI-01 | 未配置 API Key 时输入 AI 问题 | 返回未配置错误 | 输出 `AI_NOT_CONFIGURED`，主菜单继续可用 | 通过 |

#### （2）结果分析

实验 2 从用户视角验证了系统的主要业务工作流。控制台入口能够启动并退出，汇总菜单能够展示任务、日程、学习计划、收支和笔记统计；任务、日程、学习计划、收支、笔记等模块均能通过服务测试和控制台测试证明主流程正确。系统对非法输入的处理较稳定，例如空标题、非法日期范围、非法金额、无效编号和冲突日程均不会破坏已有数据。

自动化方面，项目已经形成 Maven 本地测试、JaCoCo 覆盖率和 GitHub Actions CI 的质量门禁。`.github/workflows/ci.yml` 在 push 和 pull_request 时执行 JDK 17 环境下的构建与测试，本地可通过 `mvn clean test`、`mvn clean verify` 和 `mvn jacoco:report` 获取测试结果和覆盖率证据。

实验 2 的主要风险集中在真实外部服务。当前普通测试隔离真实 DeepSeek 是合理的，但报告不能声称真实 DeepSeek 已完成集成测试。`mvn -Pintegration verify` 当前没有 `*IT.java`，只能说明集成测试入口可运行。

## 4. 缺陷记录与回归分析

| 编号 | 缺陷或风险 | 严重级别 | 根因分析 | 修复或处理方式 | 回归测试 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-01 | DeepSeek HTTP 失败映射不完整 | 一般 | 状态码和异常分类分支不足 | 补齐 `AiErrorMapper` 和 `DeepSeekAiClient` 错误映射 | `DeepSeekAiClientTest`, `AiErrorMapperTest` | 已修复 |
| BUG-05 | 任务草稿批量导入中途失败后可能留下部分任务 | 严重 | 批量导入缺少回滚语义 | `DraftImportService` 记录已创建 id 并失败回滚 | `DraftImportServiceTest` | 已修复 |
| BUG-08 | 日程首尾相接可能被误判为冲突 | 一般 | 时间区间边界定义不清 | 冲突策略采用结束点不包含的区间语义 | `ScheduleServiceTest` | 已修复 |
| BUG-14 | 汇总页缺少逾期与未来高优先级任务 | 一般 | 汇总任务视图过窄 | `SummaryService`, `DashboardSummary`, `LocalContext` 增加紧急任务视图 | `SummaryServiceTest`, `ConsoleApplicationTest` | 已修复 |
| RISK-01 | 真实 DeepSeek 连通性未作为默认测试执行 | 一般 | 默认测试有意隔离真实网络和 API Key | 记录为风险，后续可新增受 `DEEPSEEK_API_KEY` 控制的 `*IT.java` | `mvn -Pintegration verify` 仅验证入口 | 已知风险 |

回归执行结论：2026-07-09 本地执行 `mvn -q clean verify` 通过，Surefire 汇总为 992 个测试、失败 0、错误 0、跳过 0。上述缺陷均已有对应测试覆盖或风险说明。

## 5. 质量度量

| 度量项 | 当前结果 | 证据来源 |
| --- | --- | --- |
| 测试类数量 | 79 个 `*Test.java` | 文件树统计 |
| 测试通过率 | 992 / 992 = 100% | `docs/current-verification.md`, Surefire 输出 |
| 默认测试失败数 | 0 | `mvn -q clean verify` |
| 指令覆盖率 | 96.78% | `target/site/jacoco/jacoco.csv` |
| 分支覆盖率 | 86.61% | `target/site/jacoco/jacoco.csv` |
| 行覆盖率 | 94.48% | `target/site/jacoco/jacoco.csv` |
| CI 门禁 | push 和 pull_request 执行构建与测试 | `.github/workflows/ci.yml` |
| 风险残留 | 真实 DeepSeek 连通性未覆盖；当前无 `*IT.java`；内存数据不持久化 | README、环境说明、当前验证记录 |

## 6. 提交材料说明

根据 `实验说明.md`，本课程实验材料以组为单位提交，由组长填写学号和姓名信息。提交材料包括：

| 材料 | 命名要求 | 本项目对应内容 |
| --- | --- | --- |
| 实验报告 | `班级-学号-姓名-实验报告` | 本报告定稿后按要求重命名 |
| 答辩视频 | `班级-学号-姓名-答辩视频` | 展示实验 1 和实验 2 工作成果 |
| 其它文档压缩包 | `班级-学号-姓名-其它文档` | 包含待测程序源代码和单元测试脚本 |
| 待测程序 | `班级-学号-姓名-待测程序` | `java-ai-assistant/src/main/java`, `pom.xml`, `README.md`, `docs/environment.md` |
| 单元测试用例 | `班级-学号-姓名-单元测试用例` | `java-ai-assistant/src/test/java`, `docs/test-cases.md`, `docs/black-box-test-cases.md` |

提交前应重新运行 `mvn clean verify`，并将最终测试数量、失败数、覆盖率截图或摘要写入报告。

## 7. 实验总结

成员A：本次实验中，我主要负责总体测试计划、AI 问答模块和 AI 结构化建议草稿模块的测试设计。通过分析 AI 类功能，我认识到测试重点不是简单调用真实大模型，而是先保证本地上下文构造、配置加载、错误映射、结构化响应解析、草稿保存和草稿状态迁移稳定可靠。

成员B：本次实验中，我主要负责任务待办、日程提醒和控制台交互测试。任务模块看似简单，但包含空标题校验、优先级筛选、状态筛选、完成、重复完成、撤销和删除不存在记录等多个分支。日程模块则重点考察时间范围和冲突策略。

成员C：本次实验中，我主要负责学习计划、收支记录和笔记模块。学习计划的核心在于日期范围、进度边界和状态推导；收支模块的核心在于金额值对象、交易类型、日期筛选和统计计算；笔记模块则重点关注标题内容校验、关键字和标签检索。

成员D：本次实验中，我主要负责汇总统计、自动化测试、CI、覆盖率、缺陷记录和报告统稿。项目使用 Maven Surefire 执行默认单元测试，使用 JaCoCo 生成覆盖率报告，并通过 GitHub Actions 在 push 和 pull_request 时执行构建与测试。通过本次实验，我认识到软件质量保证不仅包括编写测试，还包括明确风险边界、记录缺陷、执行回归和保证报告证据可追溯。

## 成绩评定：

| 考核标准 | 得分 |
| --- | --- |
| （1）能够按时出勤，不迟到、不早退，实验过程中，具有严谨的学习态度和认真、踏实、一丝不苟的科学作风（5%） |  |
| （2）测试计划全面合理（实验1和实验2各占10%）（20%） |  |
| （3）测试用例设计合理、正确（实验1和实验2各占15%）（30%） |  |
| （4）测试结果分析充分、合理，能体现创新性（实验1和实验2各占10%）（20%） |  |
| （5）待测应用程序编码规范，实验报告规范，不与他人雷同（待测程序占15%，实验报告占10%）（25%） |  |
