* 实验一的助手应该是一个AI助手，需要接入deepseek-flash大模型，具体借口文档如下：base_url (OpenAI)：https://api.deepseek.com,model是deepseek-v4-flash。
* eq_v2 把“单元测试”与“真实文件 I/O 验证”混在了一起，测试层次不够清晰。
req_v2.md (line 77)、req_v2.md (line 109)、req_v2.md (line 117) 要求在 JUnit 中覆盖持久化/导出成功与失败路径，但课件对单元测试强调“保持在单元级别”和“不连接预定义外部资源”，更适合用 mock/stub 隔离依赖，再把真实文件读写放到组件/集成测试里验证。
风险是：测试会变慢、变脆弱，还会让“实验 1 的白盒单元测试”边界变模糊。建议把 unit 和 integration/component 两层测试明确拆开写进需求。

* 文档要求“修复并复测”，但没有把“回归测试”正式列成必须动作。
req_v2.md (line 121) 到 req_v2.md (line 125) 只要求记录失败原因、修改方式和复测结果，没有要求修复后重跑受影响测试集或关键冒烟集。课件里把回归测试明确视为缺陷修复后的标准步骤，也强调接口变化后相关接口都应回归。
这会留下“修了 A，悄悄破坏 B”的漏洞，尤其是统计汇总这种跨模块逻辑。建议补一条：每次修 Bug 必须新增复现用例，并重跑相关模块测试及一组核心回归测试。

* 白盒方法写了名称，但没有要求可证明的覆盖证据，容易流于“口头声明用了很多方法”。
req_v2.md (line 81) 到 req_v2.md (line 89) 提到语句覆盖、判定覆盖、条件覆盖、基本路径，也建议 30+ 用例；但课件进一步强调了覆盖率度量、条件/判定覆盖、条件组合覆盖、控制流图和圈复杂度驱动的基路径设计。
问题在于目前更像“数量要求”，不是“覆盖强度要求”。建议补充：至少选 2-3 个复杂核心方法画控制流图，给出圈复杂度/独立路径；并要求附 branch/condition coverage 结果或方法-用例映射表。

* 对“状态变化”和“跨模块交互”的测试要求还不够系统，偏 CRUD 清单，缺少状态迁移/场景链路视角。
虽然 req_v2.md (line 69) 提到状态变化和组合场景，但具体到各模块测试点时，req_v2.md (line 93) 到 req_v2.md (line 109) 主要还是单操作枚举。课件对这类问题专门强调“关注状态”、场景测试的基本流/备选流，以及状态迁移测试。
像“未完成→已完成→重复完成→撤销完成”“即将开始→进行中→已过期”“新增记录后汇总更新→删除后回退”这类链路，最好在需求里明确成状态迁移表或场景用例，不然很容易漏掉同步错误。