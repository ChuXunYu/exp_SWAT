# 需求文档审查报告（v3）

## 审查结果

APPROVED

## 逐维度审查

### 1. 忠实性

**[通过]** 需求文档准确保留了用户原始要求中的核心约束：Java 实现、个人助手类程序、功能不少于 8 个、课程实验 1 场景、重视测试内容、编码规范、JUnit 白盒测试、测试计划、测试结果分析和提交物要求。

**[通过]** 文档已按最新迭代要求将助手定位为 AI 助手，并明确接入 DeepSeek Flash 大模型，采用 OpenAI 兼容接口，`base_url` 为 `https://api.deepseek.com`，模型为 `deepseek-v4-flash`。

**[通过]** 文档将本轮范围限定为实验 1，与最新迭代需求中“当前范围只做实验 1”的要求一致；对于参考材料中实验 2 的内容，文档仅作为后续或模板处理背景说明，没有错误纳入本轮必须交付范围。

**[通过]** 文档对测试分层、回归测试、覆盖证据、状态变化和跨模块交互测试的补充均来自当前 todo 的明确要求，没有出现无法从原始需求或迭代需求合理推断的加戏。

### 2. 清晰性

**[通过]** 文档清楚说明了项目定位、目标用户、运行形态、AI 接入边界、必须功能、可选功能、非功能要求、测试要求、报告与提交物要求，架构设计者能够据此理解要实现的系统范围。

**[通过]** 功能边界清晰：前 8 个功能为必须完成，后 2 个为增强功能；不要求账号、云同步、复杂权限、移动端、真实系统通知、模型训练或复杂智能体框架。

**[通过]** 测试边界清晰：JUnit 单元测试需要隔离 DeepSeek API、文件系统和当前时间等外部依赖；真实文件 I/O 和真实 API 连通性被明确划入组件测试或集成测试。

**[通过]** AI 功能的可测试边界清楚，文档明确区分稳定断言对象与不稳定自然语言输出，避免下游误以为应对大模型完整回复文本做精确断言。

### 3. 完备性

**[通过]** 文档覆盖了至少 8 个可独立演示和测试的功能模块，并为每个主要功能给出了关键字段、正常流程、异常输入、边界条件和测试关注点。

**[通过]** 测试部分足够完整，覆盖测试计划、测试分层、白盒测试方法、覆盖证据、状态迁移、场景链路、用例记录、JUnit 可重复运行、缺陷修复、复测与回归测试。

**[通过]** 文档补充了 DeepSeek API Key 配置、AI 客户端封装、mock/stub/fake 使用、网络失败降级、真实接口验证归类等关键上下文，足以支撑后续设计与实现。

**[通过]** 实验报告和提交物要求完整体现实验 1 所需内容，包括源代码、运行环境说明、单元测试脚本、测试结果、覆盖证据、缺陷分析和回归结论。
