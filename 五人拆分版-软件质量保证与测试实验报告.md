课程编号：A0801050230

# 软件质量保证与测试
# 实验报告

| 组长姓名 | 成员A（替换为真实姓名） | 学 号 | 成员A学号 |
| --- | --- | --- | --- |
| 班 级 | 替换为真实班级 | 指 导 教 师 | 吴辰铌 |
| 开 设 学 期 | 2025-2026 春季学期 |  |  |
| 开 设 时 间 | 第15周--第16周 |  |  |
| 报告日期 | 2026.6.19 |  |  |
| 评 定 成 绩 |  | 评定人 | 吴辰铌 |
|  |  | 评 定 日 期 | 2026.6.19 |

东北大学软件学院

## 小组成员与任务分工

| 成员 | 建议替换信息 | 负责内容 | 主要产出 |
| --- | --- | --- | --- |
| 成员A | 姓名/学号 | 项目总体测试计划、AI 问答与 AI 草稿白盒测试、报告统稿 | 测试范围、测试策略、AI 模块用例、风险分析 |
| 成员B | 姓名/学号 | 任务待办、日程提醒模块白盒测试 | 任务/日程单元测试用例、边界与状态迁移分析 |
| 成员C | 姓名/学号 | 学习计划、收支记录模块白盒测试 | 学习/财务测试用例、统计路径与异常输入分析 |
| 成员D | 姓名/学号 | 笔记、汇总统计、控制台黑盒测试 | 查询汇总用例、CLI 系统测试、用户工作流验证 |
| 成员E | 姓名/学号 | 自动化测试、CI、覆盖率、缺陷与回归分析 | Maven/JaCoCo/CI 证据、缺陷记录、质量度量 |

## 1. 实验目的

（1）练习白盒测试用例设计，围绕 `java-ai-assistant` 项目的服务层、值对象、查询对象、AI 调用封装和状态迁移逻辑，使用 JUnit Jupiter、Mockito、Maven Surefire 和 JaCoCo 进行单元测试与覆盖证据整理。

（2）练习黑盒测试用例设计，从用户视角对控制台个人学习生活助手进行系统测试和验收测试，验证任务、日程、学习计划、收支记录、笔记、汇总统计、AI 问答和 AI 草稿确认导入等核心工作流。

（3）掌握软件质量保证过程中的测试计划、测试策略、测试用例设计、自动化测试、CI、缺陷记录、回归测试和质量度量方法，形成可复现、可追踪、可评分的实验材料。

（4）结合当前项目的真实代码、真实测试类、真实 CI 文件和验收记录完成实验报告，不编造不存在的数据库、GUI、真实 DeepSeek 集成测试或历史执行结果。

## 2. 测试计划

### 2.1 实验1测试计划

#### （1）系统功能概述

本组待测程序为 `java-ai-assistant`，项目路径为 `/root/exp_SWAT/java-ai-assistant`。该系统是一个基于 Java 17 和 Maven 的控制台个人学习生活助手，运行入口为 `assistant.app.Main`，应用装配由 `ApplicationFactory` 完成，用户通过 `ConsoleApplication` 菜单完成日常管理操作。

系统面向个人用户，主要功能包括：

| 功能模块 | 说明 | 代表性生产类 |
| --- | --- | --- |
| AI 问答与学习生活建议 | 根据本地任务、日程、学习、收支、笔记上下文构造 prompt，并通过 DeepSeek 兼容接口封装 AI 请求 | `AiAssistantService`, `PromptBuilder`, `DeepSeekAiClient`, `AiErrorMapper` |
| AI 结构化建议确认导入 | 解析 AI 返回的任务或学习计划草稿，支持查看、确认、取消、导入失败回滚 | `StructuredSuggestionParser`, `DraftLifecycleService`, `DraftImportService`, `SuggestionDraft` |
| 任务待办管理 | 支持任务新增、查询、完成、撤销、删除、优先级和状态筛选 | `TaskService`, `TaskItem`, `TaskQuery` |
| 日程提醒管理 | 支持日程新增、按日期查询、冲突检测、动态状态计算 | `ScheduleService`, `ScheduleConflictPolicy`, `ScheduleStatus` |
| 学习计划管理 | 支持学习计划创建、进度更新、状态推导、学习统计 | `StudyPlanService`, `StudyPlanAnalysisService`, `Progress` |
| 收支记录管理 | 支持收入支出记录、类别/日期筛选、收入支出结余统计 | `FinanceService`, `FinanceStatisticsService`, `MoneyValue` |
| 笔记或日记管理 | 支持笔记新增、修改、删除、关键字搜索、标签搜索 | `NoteService`, `NoteSearchPolicy`, `Tag` |
| 数据查询与汇总统计 | 汇总今日任务、今日日程、本周学习、本月收支、笔记标签和 AI 本地上下文 | `SummaryService`, `DashboardSummary`, `LocalContext` |

当前版本采用内存仓储，不包含数据库持久化、图形界面、多用户账号、系统通知和真实后台提醒。普通单元测试不访问真实 DeepSeek、真实网络、真实 API Key、真实用户文件或真实当前时间。

#### （2）测试目标

实验 1 以白盒测试为主，目标如下：

1. 验证 8 个核心功能的业务正确性，包括正常创建、查询、修改、删除、统计和汇总。
2. 验证异常输入处理，包括空标题、非法日期范围、非法金额、非法进度、无效草稿、无效 AI 响应和缺失配置。
3. 通过语句覆盖、判定覆盖、条件覆盖和基本路径测试覆盖服务层主要分支。
4. 验证任务、草稿、日程、学习计划等对象的状态迁移，确保重复确认、取消、完成、撤销等非法迁移被拒绝。
5. 验证跨模块同步，例如任务、日程、学习计划、收支和笔记变化后仪表盘摘要与 AI 本地上下文同步更新。
6. 隔离 AI 外部依赖，保证普通测试可离线、可重复执行。
7. 记录缺陷发现、修复方式、复现用例和回归验证结论。

#### （3）测试范围

实验 1 覆盖范围如下：

| 范围类型 | 内容 |
| --- | --- |
| 被测模块 | AI、AI 草稿、任务、日程、学习计划、收支、笔记、汇总、控制台入口、文档与 CI 交付 |
| 被测层级 | 服务层、实体和值对象、查询对象、策略对象、AI 客户端封装、控制台输入解析 |
| 不测范围 | 真实 DeepSeek 网络连通性、真实 API Key、数据库持久化、GUI、移动端、多用户并发、系统通知 |
| 可选扩展 | `mvn -Pintegration verify` 作为未来真实 DeepSeek `*IT.java` 集成测试入口 |

#### （4）测试环境

| 环境项 | 版本或说明 |
| --- | --- |
| 操作系统 | 开发机或 CI Linux 环境，命令以 Maven 输出为准 |
| JDK | Java 17 |
| 构建工具 | Maven |
| 单元测试框架 | JUnit Jupiter 5.14.4 |
| Mock 工具 | Mockito 5.18.0 |
| 单元测试执行 | Maven Surefire Plugin 3.5.6 |
| 集成测试入口 | Maven Failsafe Plugin 3.5.6 |
| 覆盖率工具 | JaCoCo Maven Plugin 0.8.13 |
| 项目目录 | `/root/exp_SWAT/java-ai-assistant` |

#### （5）测试工具

| 工具 | 用途 |
| --- | --- |
| JUnit Jupiter | 编写服务层、值对象、策略、AI 模块和控制台交互单元测试 |
| Mockito | 替换控制台和部分依赖协作者，验证外部服务调用边界 |
| Maven Surefire | 执行默认单元测试，匹配普通 `*Test.java` |
| Maven Failsafe | 作为可选集成测试入口，匹配 `*IT.java` |
| JaCoCo | 收集测试覆盖率并生成 HTML 报告 |
| GitHub Actions | 在 push 和 pull_request 时自动执行构建与测试 |

#### （6）测试策略

| 测试策略 | 项目化说明 |
| --- | --- |
| 语句覆盖 | 触达服务公开方法的成功路径和主要错误路径 |
| 判定覆盖 | 验证存在/不存在、合法/非法、启用/未启用、冲突/不冲突等 true/false 分支 |
| 条件覆盖 | 构造状态、优先级、日期范围、类型、标签、AI HTTP 状态族等组合输入 |
| 基本路径测试 | 分析 `FinanceStatisticsService.calculate`、`DraftImportService.importDraft`、`SummaryService.getDashboardSummary` 等复杂方法独立路径 |
| 边界值 | 覆盖进度 0/100/-1/101、金额 0/负数/两位小数、首尾相接日程、空集合统计 |
| 等价类 | 将任务优先级、日程状态、学习计划状态、交易类型、笔记标签、AI 场景划分为有效/无效等价类 |
| 错误推测 | 针对空标题、缺失 dueDate、格式异常 JSON、HTTP 401/429/5xx、超时、不存在 id 构造测试 |
| 状态迁移 | 验证任务完成/撤销、草稿确认/取消/重复确认、学习计划进度更新后的状态变化 |
| 场景链路 | 验证 AI 草稿确认导入影响任务/学习计划，业务数据变化后摘要和 AI 本地上下文同步 |

### 2.2 实验2测试计划

#### （1）测试目标

实验 2 以黑盒测试、系统测试、验收测试和自动化执行证据整理为主，目标如下：

1. 从用户视角验证控制台菜单能完成核心工作流，包括查看汇总、任务管理、日程管理、学习计划、收支记录、笔记管理、AI 问答和草稿管理。
2. 验证非法输入、无效编号、空字段、日期格式错误、冲突日程、未配置 AI 等异常场景能给出稳定反馈。
3. 验证本地 Maven 命令、JaCoCo 覆盖率报告和 GitHub Actions CI 能支撑软件质量保证闭环。
4. 结合缺陷记录模板完成缺陷严重级别、复现步骤、根因分析和回归测试说明。
5. 输出可复核的质量度量，包括测试通过率、模块用例覆盖、覆盖率、缺陷分布和风险残留。

#### （2）测试范围

| 范围类型 | 内容 |
| --- | --- |
| 黑盒主流程 | 启动退出、汇总查看、任务增删改查、日程冲突、学习计划进度、收支统计、笔记搜索、AI 未配置提示、草稿确认/取消 |
| 系统边界 | 控制台输入输出、菜单循环、错误提示、跨模块摘要同步 |
| 自动化范围 | `mvn clean test`、`mvn clean verify`、`mvn jacoco:report`、`mvn -Pintegration verify`、GitHub Actions CI |
| 不测范围 | 真实 DeepSeek 网络连通性、真实 API Key、数据库持久化、多用户并发、GUI |

#### （3）测试环境

实验 2 使用与实验 1 相同的 Java 17 和 Maven 环境。黑盒测试以控制台程序为对象，运行前在 `java-ai-assistant` 目录执行：

```bash
mvn -q -DskipTests dependency:build-classpath -Dmdep.outputFile=target/classpath.txt
mvn -q -DskipTests compile
java -cp "target/classes:$(cat target/classpath.txt)" assistant.app.Main
```

自动化验证命令如下：

```bash
mvn clean test
mvn clean verify
mvn jacoco:report
mvn -Pintegration verify
```

#### （4）测试工具

| 工具 | 用途 |
| --- | --- |
| 控制台手工输入 | 验证用户菜单、输入解析和提示信息 |
| JUnit 控制台测试 | 使用 `ConsoleApplicationTest` 验证菜单循环和异常输入 |
| Maven | 执行构建、测试和验证 |
| JaCoCo | 提供覆盖率证据 |
| GitHub Actions | 提供 CI 自动化验证 |
| 缺陷记录表 | 记录缺陷编号、严重级别、复现步骤、根因和回归范围 |

#### （5）测试策略

1. 正常场景：按用户真实操作流程输入数据并验证输出。
2. 异常场景：输入空标题、非法日期、非法金额、无效编号、冲突时间、未配置 AI，验证系统稳定拒绝。
3. 边界场景：验证进度 0 和 100、金额两位小数、日程首尾相接、空集合统计。
4. 回归场景：当缺陷修复或文档更新后，重跑关联测试类和全量 `mvn clean test`。
5. 自动化场景：将本地测试命令和 CI 工作流对应起来，说明 CI 对后续提交的质量门禁作用。

## 3. 测试用例

### 3.1 实验1测试用例及结果分析

#### （1）测试用例

实验 1 的白盒测试用例按 5 人分工组织。以下“实际结果”为当前项目测试文档与 2026-06-13 验收记录中的结果摘要，课程复核时应以现场 Maven/Surefire 输出为准。

##### 成员A：AI 问答与 AI 草稿模块

| 编号 | 测试方法 | 被测类/方法 | JUnit 测试类/方法 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| AI-01 | 基本路径 | `AiAssistantService.ask` | `AiAssistantServiceTest.askSendsBuiltRequestToClientAndReturnsContent` | fake 本地上下文、fake prompt、fake AI 返回内容 | 返回成功响应内容 | 通过 |
| AI-02 | 判定覆盖 | `AiAssistantService.ask` | `AiAssistantServiceTest.askReturnsNotConfiguredWithoutCallingCollaborators` | AI 配置未启用 | 返回未配置失败且不调用协作者 | 通过 |
| AI-03 | 边界值 | `PromptBuilder.build` | `PromptBuilderTest.buildReturnsValidationFailureForBlankQuestion` | 空白用户问题 | 返回校验失败 | 通过 |
| AI-04 | 条件覆盖 | `PromptBuilder.build` | `PromptBuilderTest.buildIncludesOverviewAndAllContextSections` | 本地任务、日程、学习、收支、笔记上下文 | system prompt 包含本地上下文 | 通过 |
| AI-05 | 等价类 | `AiConfigurationLoader.load` | `AiConfigurationLoaderTest.loadUsesDefaultsWhenMapIsEmpty` 等 | 空配置或覆盖配置 | 默认值或覆盖值正确 | 通过 |
| AI-06 | 错误推测 | `DeepSeekAiClient.chat` | `DeepSeekAiClientTest.chatMapsEmptyResponseShapes` 等 | 空 choices、缺失 message、非 JSON 响应 | 映射为空响应或格式错误 | 通过 |
| AI-07 | 条件覆盖 | `DeepSeekAiClient.chat`, `AiErrorMapper` | `DeepSeekAiClientTest.chatMapsHttpStatusFailuresWithoutParsingBody` | HTTP 401、429、5xx | 映射鉴权、限流、服务器错误 | 通过 |
| AI-08 | 错误推测 | `DeepSeekAiClient.chat`, `AiErrorMapper` | `DeepSeekAiClientTest.chatMapsTransportExceptions` | 超时、网络异常、运行时异常 | 映射外部依赖失败或系统错误 | 通过 |
| DRAFT-01 | 基本路径 | `StructuredSuggestionParser.parse` | `StructuredSuggestionParserTest.parsesTaskDraftJson` | JSON 任务草稿含标题、描述、优先级、dueDate | 解析为任务草稿 | 通过 |
| DRAFT-04 | 状态迁移 | `DraftLifecycleService.confirmDraft` | `DraftLifecycleServiceTest.confirmDraftImportsAndMarksDraftImported` | 可确认草稿、导入服务成功 | 草稿状态变为 `IMPORTED` | 通过 |
| DRAFT-08 | 回滚路径 | `DraftImportService.importDraft` | `DraftImportServiceTest.rollsBackCreatedTasksWhenTaskCreationFails` | 第二个任务创建失败 | 回滚已写入任务 | 通过 |

##### 成员B：任务待办与日程提醒模块

| 编号 | 测试方法 | 被测类/方法 | JUnit 测试类/方法 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| TASK-01 | 基本路径 | `TaskService.createTask` | `TaskServiceTest.createTaskStoresTodoTaskAndReturnsTaskView` | 标题、描述、优先级、dueDate 有效 | 创建 `TODO` 任务 | 通过 |
| TASK-02 | 边界值 | `TaskService.createTask` | `TaskServiceTest.createTaskRejectsBlankTitleAndDoesNotStoreTask` | 标题为空白 | 返回校验失败且仓储不变 | 通过 |
| TASK-03 | 条件覆盖 | `TaskService.listTasks` | `TaskServiceTest.listTasksWithStatusQueryFiltersTasks` | 按状态查询 | 只返回匹配任务 | 通过 |
| TASK-04 | 条件覆盖 | `TaskService.listTasks` | `TaskServiceTest.listTasksWithCombinedQueryFiltersTasks` | 状态、优先级、dueDate 组合查询 | 同时满足条件才返回 | 通过 |
| TASK-05 | 状态迁移 | `TaskService.markTaskCompleted` | `TaskServiceTest.markTaskCompletedChangesTodoTaskToCompleted` | `TODO` 任务 | 状态变为 `COMPLETED` | 通过 |
| TASK-06 | 状态迁移 | `TaskService.markTaskCompleted`, `TaskService.reopenTask` | `TaskServiceTest.markTaskCompletedRejectsAlreadyCompletedTaskAndKeepsState` | 已完成任务重复完成 | 重复完成失败，状态不变 | 通过 |
| SCHEDULE-01 | 基本路径 | `ScheduleService.createSchedule` | `ScheduleServiceTest.createScheduleStoresScheduleAndReturnsScheduleView` | 名称、开始、结束有效 | 创建日程并返回视图 | 通过 |
| SCHEDULE-02 | 边界值 | `ScheduleService.createSchedule` | `ScheduleServiceTest.createScheduleReturnsValidationErrorForInvalidFieldsAndDoesNotStore` | 名称为空或时间范围非法 | 返回校验失败且不存储 | 通过 |
| SCHEDULE-03 | 判定覆盖 | `ScheduleConflictPolicy` | `ScheduleServiceTest.createScheduleRejectsOverlappingScheduleAndKeepsRepositoryUnchanged` | 已有日程与新日程重叠 | 拒绝创建 | 通过 |
| SCHEDULE-04 | 边界值 | `ScheduleConflictPolicy` | `ScheduleServiceTest.createScheduleAllowsTouchingTimeRanges` | 前一日程结束等于后一日程开始 | 不视为冲突 | 通过 |
| SCHEDULE-07 | 状态迁移 | `ScheduleService.listSchedules` | `ScheduleServiceTest.listSchedulesComputesStatusesWithInjectedTimeProvider` | 固定当前时间位于日程前、中、后 | 返回即将开始、进行中、已过期状态 | 通过 |

##### 成员C：学习计划与收支记录模块

| 编号 | 测试方法 | 被测类/方法 | JUnit 测试类/方法 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| STUDY-01 | 基本路径 | `StudyPlanService.createStudyPlan` | `StudyPlanServiceTest.createStudyPlanWithoutInitialProgressDefaultsToZeroAndReturnsView` | 有效目标、日期、预期小时，未传进度 | 创建进度 0 的计划 | 通过 |
| STUDY-02 | 边界值 | `StudyPlanService.createStudyPlan` | `StudyPlanServiceTest.createStudyPlanRejectsInvalidDateRangeAndKeepsRepositoryUnchanged` | 结束日期早于开始日期 | 返回校验失败且仓储不变 | 通过 |
| STUDY-03 | 边界值 | `StudyPlanService.createStudyPlan` | `StudyPlanServiceTest.createStudyPlanAcceptsExplicitZeroInitialProgress` | 初始进度 0 | 接受并计算正确状态 | 通过 |
| STUDY-04 | 边界值 | `StudyPlanService.createStudyPlan` | `StudyPlanServiceTest.createStudyPlanRejectsNegativeInitialProgressAndKeepsRepositoryUnchanged` | 初始进度 -1 或 101 | 返回校验失败 | 通过 |
| STUDY-05 | 状态迁移 | `StudyPlanService.listStudyPlans` | `StudyPlanServiceTest.listStudyPlansComputesStatusesWithInjectedCurrentDate` | 固定当前日期覆盖未开始、进行中、完成、逾期 | 状态计算正确 | 通过 |
| FINANCE-01 | 基本路径 | `FinanceService.recordIncome`, `FinanceService.recordExpense` | `FinanceServiceTest.recordIncomeCreatesRecordAndReturnsView` | 收入、支出记录 | 创建交易并保留类型 | 通过 |
| FINANCE-02 | 边界值 | `FinanceService.addTransaction`, `TransactionAmount` | `FinanceServiceTest.addTransactionRejectsInvalidAmountAndDoesNotStore` | 金额 0、负金额 | 返回校验失败 | 通过 |
| FINANCE-04 | 条件覆盖 | `FinanceService.listTransactions` | `FinanceServiceTest.listTransactionsWithQueryFiltersByTypeCategoryDateRangeAndCombination` | 类型、类别、日期范围查询 | 返回匹配交易 | 通过 |
| FINANCE-06 | 基本路径 | `FinanceStatisticsService.calculate` | `FinanceStatisticsServiceTest.calculateReturnsZeroForEmptyRecords` 等 | 空集合、多笔收入支出 | 返回 0 统计或分别累计 | 通过 |

##### 成员D：笔记、汇总与控制台交互模块

| 编号 | 测试方法 | 被测类/方法 | JUnit 测试类/方法 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| NOTE-01 | 基本路径 | `NoteService.createNote` | `NoteServiceTest.createNoteUsesGeneratedIdAndCurrentDate` | 标题、内容、标签有效 | 创建笔记 | 通过 |
| NOTE-02 | 边界值 | `NoteService.createNote` | `NoteServiceTest.createNoteReturnsValidationErrorForInvalidInputsAndDoesNotSave` | 标题为空白 | 返回校验失败且不存储 | 通过 |
| NOTE-04 | 条件覆盖 | `NoteService.searchByKeyword` | `NoteServiceTest.searchByKeywordReturnsMatchesAndEmptyListWhenNoMatch` | 标题或正文包含关键字 | 返回匹配笔记 | 通过 |
| NOTE-06 | 条件覆盖 | `NoteService.searchByTag`, `NoteService.listNotes` | `NoteServiceTest.searchByTagUsesTagSemantics` | 标签查询 | 返回匹配标签笔记 | 通过 |
| SUMMARY-01 | 基本路径 | `SummaryService.getDashboardSummary` | `SummaryServiceTest.getDashboardSummaryQueriesServicesWithExpectedDateBoundaries` | 空内存仓储、固定日期 | 返回空摘要 | 通过 |
| SUMMARY-02 | 场景链路 | `SummaryService.getDashboardSummary` | `ConsoleApplicationTest.summaryCommandDisplaysDashboardSummary` | 同日任务/日程、本周计划、本月收支、带标签笔记 | 返回多模块组合摘要 | 通过 |
| SUMMARY-04 | 条件覆盖 | `SummaryService.getDashboardSummary` | `SummaryServiceTest.getDashboardSummaryAggregatesNoteTagsInFirstSeenOrder` | 多条笔记共享标签 | 标签分布计数正确 | 通过 |
| SUMMARY-05 | 基本路径 | `SummaryService.buildLocalContext` | `LocalContextTest.fromBuildsLinesInSourceOrderForMultiModuleData` | 已有仪表盘摘要 | 生成 AI 本地上下文 | 通过 |
| CONSOLE-01 | 黑盒/交互 | `ConsoleApplication` | `ConsoleApplicationTest` 多个菜单场景 | 菜单输入、非法编号、退出命令 | 正确展示、拒绝非法输入并可退出 | 通过 |

##### 成员E：自动化、CI、覆盖率与文档交付

| 编号 | 测试方法 | 被测对象 | 测试类或命令 | 输入或前置条件 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| AUTO-01 | 自动化执行 | 全量单元测试 | `mvn clean test` | Java 17、Maven 环境可用 | 默认测试全部通过 | 2026-06-13 历史验收记录：952 个测试通过，失败 0；现场复核以当前命令输出为准 |
| AUTO-02 | 自动化执行 | Maven 完整验证 | `mvn clean verify` | 依赖可下载 | 构建通过并生成 JaCoCo 报告 | 2026-06-13 记录：通过 |
| AUTO-03 | 覆盖率分析 | JaCoCo 报告 | `mvn jacoco:report` | 测试已执行 | 生成 `target/site/jacoco/index.html` | 2026-06-13 历史验收记录：指令 96.78%、分支 86.65%、行 94.55%；现场复核以当前命令输出为准 |
| AUTO-04 | 集成入口 | Failsafe profile | `mvn -Pintegration verify` | 当前无 `*IT.java` | 命令通过，但说明无真实集成测试 | 2026-06-13 记录：通过，No tests to run |
| AUTO-05 | CI 门禁 | GitHub Actions | `.github/workflows/ci.yml` | push 或 pull_request | 执行 `mvn -B -DskipTests package` 和 `mvn -B test` | 工作流文件存在并有文档一致性测试 |
| DOC-01 | 文档交付 | 指导书、测试计划、测试用例 | `DocumentationDeliveryTest` | 关键 Markdown 文件存在 | 文档路径和关键内容可验证 | 通过 |
| DOC-02 | CI 交付 | CI workflow | `CiWorkflowDeliveryTest` | `.github/workflows/ci.yml` 存在 | workflow 包含 JDK、构建和测试步骤 | 通过 |

#### （2）结果分析

实验 1 的白盒测试覆盖范围较广，当前项目 `src/test/java/assistant` 下共有 78 个测试类，覆盖 AI、草稿、任务、日程、学习计划、收支、笔记、汇总、应用入口、公共值对象、可测试性支持、文档和 CI 等模块。2026-06-13 完整历史验收记录显示 `mvn clean test` 共执行 952 个测试，失败 0，错误 0，跳过 0；`mvn clean verify` 通过并生成 JaCoCo 报告，历史覆盖率为指令覆盖 96.78%、分支覆盖 86.65%、行覆盖 94.55%。上述测试数量与覆盖率来自历史验收记录，课程现场复核应以当前 Maven、Surefire 和 JaCoCo 命令输出为准。

从测试充分性看，项目没有只停留在正常路径测试，而是较完整地覆盖了边界值、异常输入、状态迁移和跨模块链路。例如任务模块验证空标题拒绝、重复完成冲突和撤销；日程模块验证重叠冲突和首尾相接边界；学习计划模块验证进度 0、100、-1、101；收支模块验证非法金额和空集合统计；AI 模块验证未配置、HTTP 401/429/5xx、超时、格式异常和空响应；草稿模块验证确认、取消、重复确认和导入失败回滚。

从质量保证角度看，项目通过 `FixedTimeProvider`、`IncrementalIdGenerator`、内存仓储、fake/mock AI 依赖隔离了外部不稳定因素，使普通单元测试可以离线、重复、稳定运行。控制台测试和文档交付测试进一步补充了用户入口和提交材料的一致性检查。

风险方面，当前 `mvn -Pintegration verify` 只是集成测试入口可运行，仓库中没有 `*IT.java`，不能证明真实 DeepSeek 网络连通性已经验证。实验报告中必须明确该边界，不能将“集成 profile 通过”表述为“真实 AI 服务已通过集成测试”。此外，AI 结构化建议端到端生成草稿链路在验收记录中标记为风险项，应作为后续改进说明。

### 3.2 实验2测试用例及结果分析

#### （1）测试用例

实验 2 以黑盒和系统测试为主，测试用例按用户场景、自动化执行和缺陷回归组织。

##### 成员A：AI 用户场景与风险边界

| 编号 | 场景 | 输入或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-AI-01 | 未配置 AI 问答 | 在未配置 API Key 的环境下进入 AI 问答并输入问题 | 系统不访问真实网络，返回未配置提示 | 通过，单元测试覆盖 |
| BB-AI-02 | AI 本地上下文 | 已存在任务、日程、学习、收支、笔记数据后构造 AI 请求 | Prompt 包含本地摘要和各模块上下文 | 通过，`PromptBuilderTest` 覆盖 |
| BB-AI-03 | AI 响应异常 | 模拟空响应、格式异常、HTTP 错误、网络异常 | 系统返回稳定错误码和提示 | 通过，`DeepSeekAiClientTest` 覆盖 |
| BB-AI-04 | 草稿确认 | 存在可确认草稿时执行确认 | 导入任务或学习计划并将草稿标记为已导入 | 通过，`DraftLifecycleServiceTest` 覆盖 |
| BB-AI-05 | 草稿取消 | 存在可确认草稿时执行取消 | 草稿标记为取消，不写入业务数据 | 通过，`DraftLifecycleServiceTest` 覆盖 |

##### 成员B：任务与日程用户场景

| 编号 | 场景 | 输入或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-TASK-01 | 新增任务 | 输入有效标题、描述、优先级和截止日期 | 任务创建成功并出现在列表中 | 通过 |
| BB-TASK-02 | 空标题任务 | 标题为空白 | 系统拒绝创建并提示校验错误 | 通过 |
| BB-TASK-03 | 完成与撤销任务 | 对 TODO 任务执行完成，再执行撤销 | 状态从 TODO 到 COMPLETED，再回到 TODO | 通过 |
| BB-TASK-04 | 删除不存在任务 | 输入不存在 id | 返回未找到提示，不影响已有任务 | 通过 |
| BB-SCHEDULE-01 | 新增日程 | 输入合法开始结束时间 | 日程创建成功 | 通过 |
| BB-SCHEDULE-02 | 冲突日程 | 新建与已有日程时间重叠的日程 | 系统拒绝创建并提示冲突 | 通过 |
| BB-SCHEDULE-03 | 首尾相接日程 | 新日程开始时间等于上一日程结束时间 | 不视为冲突，允许创建 | 通过 |

##### 成员C：学习计划与收支用户场景

| 编号 | 场景 | 输入或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-STUDY-01 | 新增学习计划 | 输入目标、起止日期、预计小时 | 创建计划，默认进度为 0 | 通过 |
| BB-STUDY-02 | 非法日期范围 | 结束日期早于开始日期 | 系统拒绝创建 | 通过 |
| BB-STUDY-03 | 更新进度边界 | 输入 0、100、-1、101 | 0 和 100 接受，-1 和 101 拒绝 | 通过 |
| BB-FIN-01 | 记录收入 | 输入金额、类别、日期和备注 | 生成收入记录 | 通过 |
| BB-FIN-02 | 记录支出 | 输入金额、类别、日期和备注 | 生成支出记录 | 通过 |
| BB-FIN-03 | 非法金额 | 输入 0 或负数 | 系统拒绝记录 | 通过 |
| BB-FIN-04 | 月度统计 | 同月多笔收入支出后查看统计 | 正确显示收入、支出、结余 | 通过 |

##### 成员D：笔记、汇总与控制台主流程

| 编号 | 场景 | 输入或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-NOTE-01 | 新增笔记 | 输入标题、内容、标签 | 笔记创建成功 | 通过 |
| BB-NOTE-02 | 空标题或空内容 | 标题或内容为空白 | 系统拒绝创建 | 通过 |
| BB-NOTE-03 | 关键字搜索 | 输入标题或正文中的关键字 | 返回匹配笔记 | 通过 |
| BB-NOTE-04 | 标签搜索 | 输入已有标签 | 返回对应标签笔记 | 通过 |
| BB-SUMMARY-01 | 查看汇总 | 选择汇总菜单 | 展示今日任务、今日日程、本周学习、本月收支、笔记统计 | 通过 |
| BB-CONSOLE-01 | 启动与退出 | 执行 `printf 'q\\n' \| java -cp ... assistant.app.Main` | 主菜单正常显示并退出 | 2026-06-13 验收记录：通过 |
| BB-CONSOLE-02 | 非法菜单输入 | 输入非数字或不存在菜单项 | 系统提示非法输入并继续运行 | 通过，控制台测试覆盖 |

##### 成员E：自动化、CI 与回归场景

| 编号 | 场景 | 命令或操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| BB-AUTO-01 | 单元测试回归 | `mvn clean test` | 全量单元测试通过 | 2026-06-13 历史验收记录：952 个测试通过；现场复核以当前命令输出为准 |
| BB-AUTO-02 | 完整验证 | `mvn clean verify` | 构建通过，生成覆盖率报告 | 2026-06-13 验收记录：通过 |
| BB-AUTO-03 | 覆盖率报告 | `mvn jacoco:report` | 生成 JaCoCo HTML 报告 | 2026-06-13 验收记录：通过 |
| BB-AUTO-04 | 集成 profile | `mvn -Pintegration verify` | 命令通过，并明确当前无 `*IT.java` | 通过但 No tests to run |
| BB-AUTO-05 | CI 工作流 | push 或 pull_request 触发 `.github/workflows/ci.yml` | 执行构建和测试门禁 | workflow 已存在 |

#### （2）结果分析

实验 2 从用户视角验证了系统的主要业务工作流。控制台入口能够启动并退出，汇总菜单能够展示任务、日程、学习计划、收支和笔记统计；任务、日程、学习计划、收支、笔记等模块均能通过服务测试和控制台测试证明主流程正确。系统对非法输入的处理较稳定，例如空标题、非法日期范围、非法金额、无效编号和冲突日程均不会破坏已有数据。

自动化方面，项目已经形成 Maven 本地测试、JaCoCo 覆盖率和 GitHub Actions CI 的质量门禁。`.github/workflows/ci.yml` 在 push 和 pull_request 时执行 JDK 17 环境下的构建与测试，本地可通过 `mvn clean test`、`mvn clean verify` 和 `mvn jacoco:report` 获取测试结果和覆盖率证据。

实验 2 的主要风险集中在真实外部服务和端到端 AI 草稿链路。当前普通测试隔离真实 DeepSeek 是合理的，但报告不能声称真实 DeepSeek 已完成集成测试。验收报告中还指出 AI 结构化建议从“AI 生成草稿”到“用户确认导入”的端到端入口仍存在风险，因此在最终总结中应将其列为后续改进项，而不是隐藏风险。

## 4. 缺陷记录与回归分析

### 4.1 缺陷记录模板

| 字段 | 填写内容 |
| --- | --- |
| 缺陷编号 | BUG-AI-001 |
| 模块 | AI 结构化建议确认导入 |
| 严重级别 | 一般 |
| 环境 | Java 17、Maven、当前 main 分支 |
| 复现步骤 | 查看 CLI 草稿菜单和 AI 问答入口，检查是否存在“AI 结构化响应生成草稿并保存”的完整入口 |
| 预期结果 | 用户可从 AI 建议直接生成任务或学习计划草稿，再确认导入 |
| 实际结果 | 解析器、草稿生命周期和导入服务已实现，但验收记录指出应用层端到端生成入口不完整 |
| 根因分析 | 底层服务能力已具备，用户入口和应用服务串联仍需进一步完善 |
| 修复措施 | 后续新增结构化建议生成应用服务和 CLI 入口 |
| 回归测试 | 新增端到端服务测试和控制台测试，重跑 `mvn clean test` |
| 状态 | 延期改进 |

| 字段 | 填写内容 |
| --- | --- |
| 缺陷编号 | RISK-AI-002 |
| 模块 | DeepSeek 集成测试 |
| 严重级别 | 一般 |
| 环境 | Java 17、Maven、无真实 API Key 的默认测试环境 |
| 复现步骤 | 执行 `mvn -Pintegration verify` |
| 预期结果 | 若存在真实 `*IT.java`，可验证真实 DeepSeek 连通性 |
| 实际结果 | 命令通过，但 Failsafe 显示 `No tests to run` |
| 根因分析 | 当前项目未包含真实网络集成测试，默认测试有意隔离外部服务 |
| 修复措施 | 后续新增受 `DEEPSEEK_API_KEY` 控制的 `*IT.java` |
| 回归测试 | 设置 API Key 后执行 `mvn -Pintegration verify` |
| 状态 | 已知风险 |

### 4.2 回归测试范围

| 缺陷或风险 | 回归命令 | 关联测试 |
| --- | --- | --- |
| AI 错误映射 | `mvn -Dtest=DeepSeekAiClientTest,AiErrorMapperTest test` | AI-06、AI-07、AI-08 |
| AI 草稿导入回滚 | `mvn -Dtest=DraftImportServiceTest,DraftLifecycleServiceTest test` | DRAFT-04、DRAFT-08 |
| 任务状态迁移 | `mvn -Dtest=TaskServiceTest test` | TASK-05、TASK-06 |
| 日程冲突 | `mvn -Dtest=ScheduleServiceTest,ScheduleConflictPolicyTest test` | SCHEDULE-03、SCHEDULE-04 |
| 财务统计 | `mvn -Dtest=FinanceServiceTest,FinanceStatisticsServiceTest test` | FINANCE-04、FINANCE-06 |
| 汇总同步 | `mvn -Dtest=SummaryServiceTest,ConsoleApplicationTest test` | SUMMARY-02、BB-SUMMARY-01 |
| 文档和 CI 交付 | `mvn -Dtest=DocumentationDeliveryTest,CiWorkflowDeliveryTest test` | DOC-01、DOC-02 |
| 全量回归 | `mvn clean test` | 所有默认单元测试 |

## 5. 质量度量

| 度量项 | 结果或记录方式 | 证据来源 |
| --- | --- | --- |
| 测试类数量 | 当前 `src/test/java/assistant` 下 78 个 `*Test.java` 文件 | 文件树统计 |
| 测试通过率 | 2026-06-13 历史验收记录：952 / 952 = 100%；现场复核以当前命令输出为准 | `acceptance/20260613_full_acceptance.md` |
| 默认测试失败数 | 0 | `mvn clean test` 历史验收记录 |
| 指令覆盖率 | 96.78%，现场复核以当前 JaCoCo 输出为准 | 2026-06-13 JaCoCo 历史验收记录 |
| 分支覆盖率 | 86.65%，现场复核以当前 JaCoCo 输出为准 | 2026-06-13 JaCoCo 历史验收记录 |
| 行覆盖率 | 94.55%，现场复核以当前 JaCoCo 输出为准 | 2026-06-13 JaCoCo 历史验收记录 |
| CI 门禁 | push 和 pull_request 执行构建与测试 | `.github/workflows/ci.yml` |
| 风险残留 | 真实 DeepSeek 连通性未覆盖；当前无 `*IT.java`；内存数据不持久化；AI 结构化建议端到端入口存在风险 | 指导书与验收报告 |

## 6. 五人拆分提交建议

为避免 5 份报告完全雷同，建议每人保留公共章节 1、2、4、5，但在第 3 章重点保留自己负责模块，并在实验总结中突出个人工作。

| 成员 | 建议保留重点 | 可删减内容 |
| --- | --- | --- |
| 成员A | AI、AI 草稿、总体测试计划、风险分析 | 可简化任务/日程/学习/财务细节 |
| 成员B | 任务、日程、状态迁移和边界值 | 可简化 AI 和 CI 细节 |
| 成员C | 学习计划、收支记录、统计路径和异常输入 | 可简化控制台和文档交付细节 |
| 成员D | 笔记、汇总、控制台黑盒测试、用户场景 | 可简化底层 AI HTTP 错误映射 |
| 成员E | 自动化测试、CI、覆盖率、缺陷与回归 | 可简化各业务模块白盒表格 |

## 7. 实验总结

成员A：本次实验中，我主要负责总体测试计划、AI 问答模块和 AI 结构化建议草稿模块的测试设计。通过分析 `AiAssistantService`、`PromptBuilder`、`DeepSeekAiClient`、`StructuredSuggestionParser`、`DraftLifecycleService` 和 `DraftImportService`，我认识到 AI 类功能的测试重点不是简单调用真实大模型，而是要先保证本地上下文构造、配置加载、错误映射、结构化响应解析和草稿状态迁移稳定可靠。实验中我将未配置 API Key、HTTP 401/429/5xx、超时、空响应、格式异常、重复确认和导入失败回滚作为重点场景，避免测试依赖真实网络环境。通过本次实验，我对外部依赖隔离、错误推测测试和状态迁移测试有了更完整的理解，也认识到当前项目仍需补充真实 DeepSeek 集成测试和 AI 生成草稿端到端入口。

成员B：本次实验中，我主要负责任务待办和日程提醒模块的白盒测试。任务模块看似简单，但实际包含空标题校验、优先级筛选、状态筛选、完成、重复完成、撤销和删除不存在记录等多个分支。日程模块则重点考察时间范围和冲突策略，尤其是“时间重叠应拒绝”和“首尾相接不冲突”两个边界。通过设计 `TaskServiceTest`、`ScheduleServiceTest` 和 `ScheduleConflictPolicyTest` 相关用例，我进一步理解了边界值分析、判定覆盖和状态迁移测试的作用。实验让我认识到，高质量测试不仅要验证功能能成功执行，也要验证非法操作不会破坏已有数据。

成员C：本次实验中，我主要负责学习计划和收支记录模块。学习计划模块的核心在于日期范围、进度边界和状态推导，因此我重点分析了进度 0、100、-1、101，以及未开始、进行中、完成、逾期等状态。收支模块的核心在于金额值对象、交易类型、日期筛选和统计计算，因此我重点验证了金额 0、负数、两位小数、空集合统计、多笔收入支出和支出大于收入等场景。通过本次实验，我体会到值对象可以把校验逻辑集中起来，提高测试可读性和可靠性；统计类方法则适合用基本路径测试覆盖空输入、单条记录、多条记录和异常边界。

成员D：本次实验中，我主要负责笔记、汇总统计和控制台黑盒测试。笔记模块的测试重点包括标题和内容校验、关键字搜索、标签搜索、无匹配结果和删除不存在记录。汇总模块需要从多个服务读取数据，生成今日任务、今日日程、本周学习、本月收支和笔记标签统计，因此我重点关注跨模块数据同步和日期边界。控制台黑盒测试让我认识到用户输入并不总是规范的，系统需要处理非数字、空输入、非法编号和退出命令等情况。通过本次实验，我理解了系统测试应从用户工作流出发，而不仅仅从单个类的方法出发。

成员E：本次实验中，我主要负责自动化测试、CI、覆盖率、缺陷记录和回归分析。项目使用 Maven Surefire 执行默认单元测试，使用 JaCoCo 生成覆盖率报告，并通过 GitHub Actions 在 push 和 pull_request 时执行构建与测试。根据 2026-06-13 历史验收记录，项目 `mvn clean test` 执行 952 个测试且全部通过，JaCoCo 指令覆盖率 96.78%、分支覆盖率 86.65%、行覆盖率 94.55%；这些数字用于报告证据引用，现场复核仍以当前命令输出为准。同时，我也注意到 `mvn -Pintegration verify` 当前没有 `*IT.java`，不能等同于真实 DeepSeek 集成测试通过。通过本次实验，我认识到软件质量保证不仅包括编写测试，还包括明确风险边界、建立 CI 门禁、记录缺陷、执行回归测试和保证报告证据可追溯。

## 成绩评定

| 考核标准 | 自评说明 | 得分 |
| --- | --- | --- |
| （1）能够按时出勤，不迟到、不早退，实验过程中，具有严谨的学习态度和认真、踏实、一丝不苟的科学作风（5%） | 小组 5 人按分工完成测试计划、用例设计、自动化验证和报告整理，报告中明确区分真实结果和历史验收记录。 |  |
| （2）测试计划全面合理（实验1和实验2各占10%）（20%） | 实验 1 覆盖白盒单元测试策略，实验 2 覆盖黑盒、系统、验收、CI 和缺陷回归策略，范围、环境、工具和风险说明完整。 |  |
| （3）测试用例设计合理、正确（实验1和实验2各占15%）（30%） | 用例覆盖 AI、草稿、任务、日程、学习、收支、笔记、汇总、控制台、自动化和 CI，包含语句、判定、条件、基本路径、边界、等价类、状态迁移和错误推测。 |  |
| （4）测试结果分析充分、合理，能体现创新性（实验1和实验2各占10%）（20%） | 结果分析结合 952 个测试通过、JaCoCo 覆盖率、CI、真实 DeepSeek 未覆盖风险和 AI 草稿端到端风险，未夸大测试结论。 |  |
| （5）待测应用程序编码规范，实验报告规范，不与他人雷同（待测程序占15%，实验报告占10%）（25%） | 项目分包清晰、测试资产充分，报告按 5 人分工组织，可分别保留不同模块重点后提交。 |  |
