课程编号：A0801050230

# 软件质量保证与测试
# java-ai-assistant 实验指导书

东北大学软件学院

## 一、实验基本信息

| 项目 | 内容 |
|------|------|
| 课程名称 | 软件质量保证与测试 |
| 实验对象 | `/root/exp_SWAT/java-ai-assistant` |
| 项目类型 | Java 17、Maven 单模块、控制台个人学习生活助手 |
| 运行入口 | `assistant.app.Main` |
| 实验学时 | 白盒测试 4 学时，黑盒/系统测试 4 学时 |
| 主要交付 | 实验报告、答辩视频、待测程序源码、单元测试脚本、测试计划与测试用例证据 |

本指导书面向当前仓库中的真实 `java-ai-assistant` 项目。所有路径、类名、命令和测试资产均以 `/root/exp_SWAT` 当前文件树为依据。历史测试数量和覆盖率只能作为验收记录引用，课程复核时以学生现场执行 Maven、Surefire、Failsafe 和 JaCoCo 的输出为准。

## 二、实验目的

1. 理解真实 Java 项目的测试对象、测试范围、质量属性和风险边界。
2. 使用 JUnit Jupiter、Mockito、Maven Surefire、Maven Failsafe 和 JaCoCo 复核白盒测试、边界测试、异常测试和回归测试。
3. 面向控制台个人助手业务设计黑盒测试、系统测试和验收测试场景。
4. 掌握缺陷记录、缺陷严重度分类、回归验证、测试通过率和覆盖率证据整理方法。
5. 按课程模板完成实验报告，并能用 CI 与命令行输出支撑评分。

## 三、实验环境

| 环境项 | 要求 |
|--------|------|
| JDK | Java 17 |
| 构建工具 | Maven |
| 单元测试 | JUnit Jupiter 5.14.4 |
| Mock 工具 | Mockito 5.18.0 |
| 测试执行 | Maven Surefire Plugin 3.5.6 |
| 集成入口 | Maven Failsafe Plugin 3.5.6 |
| 覆盖率 | JaCoCo Maven Plugin 0.8.13 |
| 工作目录 | `/root/exp_SWAT/java-ai-assistant` |

默认测试不访问真实 DeepSeek、真实网络、真实 API Key、用户文件或真实当前时间。项目通过内存仓储、`TimeProvider` 和 `IdGenerator` 保持测试隔离性。

## 四、项目简介与被测对象

`java-ai-assistant` 是单用户控制台个人学习生活助手。项目使用 Java 17 和 Maven 单模块结构，入口类为 `assistant.app.Main`，启动后由 `ApplicationFactory` 装配服务，`ConsoleApplication` 提供菜单交互，`DemoDataFactory` 默认加载演示数据。

当前版本使用内存仓储，不包含数据库、文件导出、系统通知、账号、多用户、图形界面或后台提醒服务。默认 AI 测试只验证本地上下文构造、配置加载、错误映射和 DeepSeek 客户端封装，不验证真实 DeepSeek 网络连通性。

| 证据项 | 真实路径或类名 | 用途 | 边界说明 |
|--------|----------------|------|----------|
| 项目入口 | `/root/exp_SWAT/java-ai-assistant/src/main/java/assistant/app/Main.java` | 运行控制台程序 | 单用户 CLI |
| 应用装配 | `ApplicationFactory`, `ApplicationServices`, `ConsoleApplication` | 系统测试和装配复核 | 不含 Web 或 GUI |
| README | `/root/exp_SWAT/java-ai-assistant/README.md` | 环境、命令、功能边界 | 历史数字需以当前输出复核 |
| 测试计划 | `/root/exp_SWAT/java-ai-assistant/docs/test-plan.md` | 测试策略证据 | 具体测试数量以 Surefire 为准 |
| 白盒用例 | `/root/exp_SWAT/java-ai-assistant/docs/test-cases.md` | 用例编号和模块映射 | 实际结果需复跑确认 |
| CI 文件 | `/root/exp_SWAT/.github/workflows/ci.yml` | 自动化验证依据 | 在 `java-ai-assistant` 目录执行 Maven |
| 验收记录 | `/root/exp_SWAT/acceptance/20260613_full_acceptance.md` | 历史测试与覆盖率证据 | 仅代表 2026-06-13 记录 |

核心业务模块与代表性生产类如下：

| 模块 | 代表性生产类 |
|------|--------------|
| AI 与草稿 | `AiAssistantService`, `PromptBuilder`, `DeepSeekAiClient`, `AiConfigurationLoader`, `StructuredSuggestionParser`, `StructuredSuggestionDraftService`, `DraftLifecycleService`, `DraftImportService` |
| 应用入口与控制台 | `Main`, `ApplicationFactory`, `ConsoleApplication`, `DemoDataFactory` |
| 任务 | `TaskService`, `TaskItem`, `TaskQuery`, `TaskStatus`, `TaskPriority` |
| 日程 | `ScheduleService`, `ScheduleItem`, `ScheduleConflictPolicy`, `ScheduleStatus`, `ScheduleQuery` |
| 学习计划 | `StudyPlanService`, `StudyPlan`, `StudyPlanAnalysisService`, `StudyPlanStatus`, `StudyPlanQuery` |
| 收支 | `FinanceService`, `FinanceStatisticsService`, `TransactionRecord`, `TransactionQuery`, `TransactionType` |
| 笔记 | `NoteService`, `Note`, `NoteQuery`, `NoteSearchPolicy` |
| 汇总 | `SummaryService`, `DashboardSummary`, `LocalContext` |
| 可测试性 | `TimeProvider`, `FixedTimeProvider`, `IdGenerator`, `IncrementalIdGenerator` |

## 五、被测范围与不测范围

| 类别 | 范围 |
|------|------|
| 被测范围 | 任务、日程、学习计划、收支、笔记、汇总、AI 问答封装、AI 草稿解析与确认导入、控制台菜单、文档和 CI 交付 |
| 默认不测范围 | 真实 DeepSeek 网络连通性、真实 API Key、数据库持久化、文件导出、系统通知、账号体系、多用户隔离、GUI、移动端 |
| 可选扩展 | 使用 `mvn -Pintegration verify` 作为未来真实 DeepSeek `*IT.java` 集成测试入口 |

## 六、质量目标与质量属性

| 质量属性 | 目标 | 观察方式 |
|----------|------|----------|
| 正确性 | 核心功能创建、查询、更新、删除、统计、汇总符合预期 | JUnit 断言、黑盒场景、CLI smoke |
| 健壮性 | 空标题、非法日期、非法金额、非法进度、无效 AI 响应能稳定拒绝 | 异常场景和错误推测用例 |
| 可测试性 | 外部时间、编号、AI 和 HTTP 依赖可替换 | `FixedTimeProvider`, `IncrementalIdGenerator`, fake/mock AI |
| 可维护性 | 分包清晰、服务与控制台分离、值对象集中校验 | 代码审查、类索引、测试映射 |
| 可追溯性 | 测试计划、用例、缺陷、覆盖率和 CI 结果互相引用 | 文档路径、Surefire、JaCoCo、CI 日志 |

## 七、测试策略

| 测试层级 | 目标 | 入口或证据 |
|----------|------|------------|
| 单元测试 | 覆盖服务、实体、值对象、查询、AI 请求构造和错误映射 | `mvn clean test` |
| 控制台交互单元测试 | 覆盖菜单循环、输入解析、错误提示和摘要展示 | `ConsoleApplicationTest` |
| 集成测试入口 | 预留真实 DeepSeek 或跨进程集成测试入口 | `mvn -Pintegration verify`；当前无 `*IT.java` |
| 系统/验收测试 | 从用户工作流复核任务、日程、学习、财务、笔记、AI 草稿和汇总 | CLI smoke、验收记录、实验报告 |
| CI 自动化 | 在 push 和 pull_request 时复核构建与测试 | `.github/workflows/ci.yml` |

测试类型至少覆盖：

| 类型 | 项目化示例 |
|------|------------|
| 白盒测试 | 服务公开方法的语句、判定、条件和基本路径覆盖 |
| 黑盒测试 | 从用户菜单输入验证任务、日程、收支、笔记和汇总输出 |
| 边界值 | 进度 0/100/-1/101，金额 0/负数/两位小数，日程首尾相接 |
| 等价类 | 任务优先级、日程状态、学习计划状态、交易类型、笔记标签 |
| 判定表或状态迁移 | 任务完成/撤销，草稿确认/取消/重复确认，学习计划状态变化 |
| 异常场景 | 未配置 AI、HTTP 401/429/5xx、超时、格式异常 JSON、不存在 id |
| 回归测试 | 缺陷修复后重跑关联测试类、模块命令和全量 Maven 命令 |

准入标准：JDK 与 Maven 可用，依赖可下载，关键路径存在，默认测试不依赖真实外部服务。准出标准：默认测试全部通过，缺陷记录闭环，覆盖率证据或当前执行输出可复核，实验报告材料齐全。

## 八、测试计划

实验报告中的测试计划应结合 `/root/exp_SWAT/java-ai-assistant/docs/test-plan.md` 编写，并至少包含以下内容：

| 项 | 填写要求 |
|----|----------|
| 系统功能概述 | 说明本项目面向个人学习生活管理，覆盖 AI、任务、日程、学习、收支、笔记和汇总 |
| 测试目标 | 说明要验证的功能正确性、异常处理、外部依赖隔离、覆盖率和缺陷闭环 |
| 测试范围 | 明确被测模块和不测范围，不得声称已有数据库、GUI 或真实 DeepSeek 集成测试 |
| 测试环境 | 写明 Java 17、Maven、JUnit、Mockito、Surefire、Failsafe、JaCoCo |
| 测试工具 | 说明 JUnit、Mockito、Maven、JaCoCo、GitHub Actions 的用途 |
| 测试策略 | 按白盒、黑盒、边界、等价类、状态迁移、异常、回归和 CI 组织 |
| 风险与应对 | AI 真实连通性未覆盖、内存数据不持久化、历史测试数字需现场复核 |

## 九、测试用例设计方法

| 方法 | 适用模块 | 示例输入 | 预期关注点 |
|------|----------|----------|------------|
| 语句覆盖 | `TaskService`, `FinanceService`, `NoteService` | 创建、查询、删除记录 | 主要语句被执行 |
| 判定覆盖 | `ScheduleConflictPolicy`, `DraftLifecycleService` | 冲突/不冲突，终态/非终态草稿 | true/false 分支均覆盖 |
| 条件覆盖 | `TaskQuery`, `TransactionQuery`, `NoteQuery` | 多字段组合筛选 | 组合条件正确过滤 |
| 基本路径 | `FinanceStatisticsService.calculate`, `DraftImportService.importDraft` | 空集合、多笔记录、导入失败 | 独立路径可追溯 |
| 边界值 | `Progress`, `MoneyValue`, `DateRange` | 0、100、-1、101；非法日期范围 | 边界接受或拒绝 |
| 等价类 | 任务优先级、交易类型、标签 | 有效枚举、无效输入 | 分类处理一致 |
| 状态迁移 | `TaskItem`, `SuggestionDraft`, `StudyPlan` | 完成、撤销、确认、取消 | 合法迁移成功，非法迁移拒绝 |
| 错误推测 | `DeepSeekAiClient`, `AiErrorMapper` | 空响应、非 JSON、HTTP 错误 | 错误码和提示稳定 |

## 十、单元测试实验

所有命令均在 `/root/exp_SWAT/java-ai-assistant` 执行。

```bash
mvn clean test
```

实验步骤：

1. 阅读 `docs/test-plan.md` 和 `docs/test-cases.md`，确认模块、用例编号、测试类和预期结果。
2. 运行 `mvn clean test`，记录 Surefire 输出中的 tests、failures、errors、skipped。
3. 选择至少 3 个模块展开分析，例如任务、日程、AI 草稿、收支或汇总。
4. 对每个模块说明测试是否隔离外部依赖，是否覆盖成功、失败、边界、异常和状态迁移。
5. 若发现失败，按“缺陷记录与分析”模板登记，并重跑关联测试类或全量测试。

模块观察点：

| 模块 | 代表性测试类 | 观察点 |
|------|--------------|--------|
| AI | `AiAssistantServiceTest`, `PromptBuilderTest`, `DeepSeekAiClientTest`, `AiConfigurationLoaderTest` | 未配置 AI 不调用外部服务，HTTP/网络/格式错误映射稳定 |
| 草稿 | `StructuredSuggestionParserTest`, `DraftLifecycleServiceTest`, `DraftImportServiceTest` | 草稿解析、确认、取消、重复确认和导入失败回滚 |
| 任务 | `TaskServiceTest`, `TaskItemTest`, `TaskQueryTest` | 创建、筛选、完成、撤销、删除不存在 |
| 日程 | `ScheduleServiceTest`, `ScheduleConflictPolicyTest`, `ScheduleStatusTest` | 时间范围、冲突识别、首尾相接、动态状态 |
| 学习计划 | `StudyPlanServiceTest`, `StudyPlanAnalysisServiceTest`, `ProgressTest` | 日期范围、进度边界、状态和统计 |
| 收支 | `FinanceServiceTest`, `FinanceStatisticsServiceTest`, `MoneyValueTest`, `TransactionQueryTest` | 金额边界、日期筛选、收入支出结余 |
| 笔记 | `NoteServiceTest`, `NoteSearchPolicyTest`, `TagTest` | 标题内容校验、关键字、标签匹配 |
| 汇总 | `SummaryServiceTest`, `DashboardSummaryTest`, `LocalContextTest` | 多模块摘要、日期边界、AI 本地上下文 |

## 十一、集成测试实验

当前可用入口：

```bash
mvn -Pintegration verify
```

当前仓库没有 `*IT.java` 文件。若命令通过且 Failsafe 输出 `No tests to run`，只能说明集成测试入口可执行，不等价于真实 DeepSeek 连通性已验证。

本实验应说明以下边界：

| 场景 | 当前处理方式 |
|------|--------------|
| 服务组合 | 通过 `ApplicationFactoryTest`, `ConsoleApplicationTest`, `SummaryServiceTest` 等局部复核 |
| AI 配置加载 | `AiConfigurationLoaderTest` 验证默认值、覆盖值和非法 timeout |
| 草稿导入 | `DraftImportServiceTest` 验证任务和学习计划导入及回滚 |
| 摘要同步 | `SummaryServiceTest` 和控制台测试验证业务变化后摘要更新 |
| 真实 DeepSeek 连通性 | 未来可新增 `*IT.java`，并要求网络与 `DEEPSEEK_API_KEY` |

## 十二、系统测试与验收测试实验

系统测试从用户工作流组织，不要求学生修改生产源码。

推荐验收路径：

| 工作流 | 测试目标 | 证据 |
|--------|----------|------|
| 启动与退出 | 程序可进入主菜单并退出 | `printf 'q\n' \| java -cp "target/classes:$(cat target/classpath.txt)" assistant.app.Main` |
| 汇总查看 | 今日任务、逾期未完成任务、未来 7 天高优先级任务、日程、本周学习、本月收支、笔记统计可显示 | 控制台输出或 `ConsoleApplicationTest` |
| 任务管理 | 新增、查看、筛选、完成、撤销、删除 | CLI 记录或 `TaskServiceTest` |
| 日程管理 | 新增、冲突拒绝、按日期查看、删除 | CLI 记录或 `ScheduleServiceTest` |
| 学习计划 | 新增、更新进度、状态推导、统计 | CLI 记录或 `StudyPlanServiceTest` |
| 收支管理 | 记录收入支出、筛选、统计结余 | CLI 记录或 `FinanceServiceTest` |
| 笔记管理 | 新增、关键字搜索、标签搜索、更新删除 | CLI 记录或 `NoteServiceTest` |
| AI 问答 | 未配置 API Key 时返回未配置错误 | CLI 记录或 `AiAssistantServiceTest` |
| AI 草稿 | 生成、查看、确认、取消结构化建议草稿 | `StructuredSuggestionDraftServiceTest`, `DraftLifecycleServiceTest`, `DraftImportServiceTest` |

2026-06-13 早期验收记录 `/root/exp_SWAT/acceptance/20260613_full_acceptance.md` 中记录：`mvn clean test` 952 个测试通过，`mvn clean verify` 通过，`mvn -Pintegration verify` 通过但无集成测试可运行，JaCoCo 指令覆盖 96.78%、分支覆盖 86.65%、行覆盖 94.55%。随后 QA 风险修复已完成，最新记录 `/root/exp_SWAT/implements/202606131017_qa-risk-fixes/verify_v4.md` 显示 `mvn clean verify` 通过，989 个测试通过、失败 0、错误 0、跳过 0；当前本地 JaCoCo 汇总为指令覆盖 96.79%、分支覆盖 86.61%、行覆盖 94.48%。课程复核时必须以当前执行输出为准。

## 十三、自动化测试与 CI 实验

CI 文件为 `/root/exp_SWAT/.github/workflows/ci.yml`。其触发条件为 `push` 和 `pull_request`，运行环境为 Ubuntu latest，JDK 17 使用 Temurin，默认工作目录是 `java-ai-assistant`。

CI 步骤包含：

```bash
mvn -B -DskipTests package
mvn -B test
```

实验报告应说明本地命令与 CI 命令的关系：本地 `mvn clean test` 用于全量单元测试复核，`mvn clean verify` 用于完整 Maven 验证和覆盖率生成，CI 负责在代码提交或合并请求时自动执行构建和测试。

## 十四、缺陷记录与分析

缺陷记录模板：

| 字段 | 填写说明 |
|------|----------|
| 缺陷编号 | 如 `BUG-TASK-001` |
| 模块 | AI、任务、日程、学习计划、收支、笔记、汇总、控制台、文档或 CI |
| 严重级别 | 阻塞、严重、一般、轻微 |
| 环境 | JDK、Maven、操作系统、命令、分支或提交 |
| 复现步骤 | 从干净状态开始列出可重复步骤 |
| 预期结果 | 需求或测试用例规定的行为 |
| 实际结果 | 失败输出、异常、截图或日志摘要 |
| 根因分析 | 代码、设计、数据、环境或文档原因 |
| 修复措施 | 修改点、测试补充或文档修正 |
| 回归测试 | 重跑命令和测试类 |
| 状态 | 新建、处理中、已修复、已验证、关闭、延期 |

严重级别示例：

| 级别 | 项目化示例 |
|------|------------|
| 阻塞 | `mvn clean test` 无法启动或主程序无法启动 |
| 严重 | 任务、日程、收支等核心数据错误或草稿导入破坏数据一致性 |
| 一般 | 某个边界输入提示不清晰、单个筛选条件错误 |
| 轻微 | 文档历史数字未同步、输出格式不影响功能 |

## 十五、质量度量

| 度量项 | 计算或记录方式 | 证据来源 |
|--------|----------------|----------|
| 测试通过率 | `通过测试数 / 执行测试数 * 100%` | Maven Surefire 输出 |
| 模块用例覆盖范围 | 已覆盖模块数、代表性测试类、用例编号 | `docs/test-cases.md` 和测试类索引 |
| 覆盖率 | 指令、分支、行覆盖率 | `mvn clean verify`, `mvn jacoco:report`, `target/site/jacoco/index.html` |
| 缺陷分布 | 按模块和严重级别统计 | 缺陷记录表 |
| 自动化执行结果 | 本地命令与 CI 结果是否通过 | Maven 输出和 GitHub Actions |
| 风险残留 | 未覆盖真实 DeepSeek、无 `*IT.java`、内存数据不持久化 | QA 修复验证记录、README 和环境说明 |

不得伪造新的执行结果。报告中的测试数量、失败数、覆盖率和 CI 状态必须来自当前真实执行输出，或明确标注为“来自 2026-06-13 验收记录”。

## 十六、实验报告提交要求

提交要求参考 `/root/exp_SWAT/实验说明.md` 和 `/root/exp_SWAT/实验报告模板.md`。

| 材料 | 要求 |
|------|------|
| 实验报告 | 命名为“班级-学号-姓名-实验报告”，按模板填写测试计划、测试用例、结果分析和总结 |
| 答辩视频 | 实验 1 和实验 2 可录制在同一个视频中，展示工作成果和测试证据 |
| 其它文档 | 待测程序源代码和单元测试脚本压缩提交，标注开发和运行环境 |
| 待测程序 | 可使用 `/root/exp_SWAT/java-ai-assistant` 当前项目 |
| 单元测试用例 | 可引用 `src/test/java`、`docs/test-cases.md` 和 Maven 输出 |

报告中应删除模板说明文字，保留真实路径、命令、测试输出摘要、缺陷记录和质量度量。若小组人数不超过 4 人，实验材料以组为单位提交，由组长填写学号和姓名信息。

## 十七、评分标准

| 评分项 | 分值 | 高分要求 | 扣分点 |
|--------|------|----------|--------|
| 出勤与实验态度 | 5 | 按时参与实验，记录真实，复核过程完整 | 缺席、迟到早退、记录敷衍 |
| 项目理解与范围界定 | 10 | 准确说明 Java 17、Maven、CLI、内存仓储、AI 边界和不测范围 | 写成通用教材，声称不存在的数据库、GUI 或真实 AI 测试 |
| 测试计划 | 15 | 目标、范围、环境、工具、策略、风险和准入准出完整 | 缺少真实路径、策略泛化、未说明外部依赖隔离 |
| 白盒测试用例设计 | 15 | 覆盖语句、判定、条件、基本路径、边界、异常和状态迁移 | 只列功能点，无测试方法或 JUnit 映射 |
| 黑盒与系统场景 | 10 | 从用户工作流设计 CLI 场景，覆盖核心模块和验收路径 | 无输入输出、未覆盖主业务、不可复现 |
| 自动化执行与 CI | 10 | 本地 Maven、Surefire、Failsafe、JaCoCo 和 CI 证据清晰 | 未运行命令，混淆空集成测试与真实连通性 |
| 缺陷分析与回归 | 10 | 缺陷模板完整，严重度合理，修复和回归测试闭环 | 缺陷无复现步骤、无根因或无回归证据 |
| 质量度量 | 10 | 通过率、覆盖率、模块覆盖、缺陷分布和风险残留可追溯 | 伪造数字，未标注历史来源，无法复核 |
| 报告规范与材料完整 | 10 | 命名、格式、路径、截图或输出摘要规范，材料齐全 | 保留模板说明，路径不存在，材料缺失 |
| 代码与测试资产规范 | 5 | 待测程序和测试脚本结构清晰，命名规范，能复跑 | 源码或测试缺失、无法构建、与报告不一致 |
| 合计 | 100 |  |  |

## 附录 A：推荐命令

以下命令均在 `/root/exp_SWAT/java-ai-assistant` 执行：

```bash
mvn clean test
mvn clean verify
mvn jacoco:report
mvn -Pintegration verify
mvn -q -DskipTests dependency:build-classpath -Dmdep.outputFile=target/classpath.txt
mvn -q -DskipTests compile
java -cp "target/classes:$(cat target/classpath.txt)" assistant.app.Main
printf 'q\n' | java -cp "target/classes:$(cat target/classpath.txt)" assistant.app.Main
```

## 附录 B：关键目录结构

| 路径 | 说明 |
|------|------|
| `/root/exp_SWAT/java-ai-assistant/pom.xml` | Maven 项目配置 |
| `/root/exp_SWAT/java-ai-assistant/src/main/java/assistant` | 生产代码 |
| `/root/exp_SWAT/java-ai-assistant/src/test/java/assistant` | JUnit 测试代码 |
| `/root/exp_SWAT/java-ai-assistant/docs/test-plan.md` | 测试计划 |
| `/root/exp_SWAT/java-ai-assistant/docs/test-cases.md` | 白盒测试用例 |
| `/root/exp_SWAT/java-ai-assistant/docs/defect-regression.md` | 缺陷与回归记录 |
| `/root/exp_SWAT/java-ai-assistant/docs/coverage/README.md` | 覆盖率证据说明 |
| `/root/exp_SWAT/.github/workflows/ci.yml` | GitHub Actions CI |
| `/root/exp_SWAT/acceptance/20260613_full_acceptance.md` | 历史验收报告 |
| `/root/exp_SWAT/实验报告模板.md` | 课程实验报告模板 |
| `/root/exp_SWAT/实验说明.md` | 提交材料说明 |

## 附录 C：关键测试类索引

| 模块 | 被测对象 | 代表性测试类 | 观察点 |
|------|----------|--------------|--------|
| AI | `AiAssistantService`, `PromptBuilder`, `DeepSeekAiClient`, `AiConfigurationLoader` | `AiAssistantServiceTest`, `PromptBuilderTest`, `DeepSeekAiClientTest`, `AiConfigurationLoaderTest`, `AiErrorMapperTest` | 请求构造、上下文注入、紧急任务上下文、配置默认值、错误映射 |
| AI 草稿 | `StructuredSuggestionParser`, `StructuredSuggestionDraftService`, `DraftLifecycleService`, `DraftImportService`, `SuggestionDraft` | `StructuredSuggestionParserTest`, `StructuredSuggestionDraftServiceTest`, `DraftLifecycleServiceTest`, `DraftImportServiceTest`, `SuggestionDraftTest` | JSON 解析、结构化生成、dueDate 校验、确认、取消、重复确认、breakdown 落地、回滚 |
| 应用入口 | `Main`, `ApplicationFactory`, `ConsoleApplication`, `DemoDataFactory` | `MainTest`, `ApplicationFactoryTest`, `ConsoleApplicationTest`, `DemoDataFactoryTest` | 装配、菜单循环、输入解析、演示数据 |
| 任务 | `TaskService`, `TaskItem`, `TaskQuery`, `TaskStatus`, `TaskPriority` | `TaskServiceTest`, `TaskItemTest`, `TaskQueryTest`, `TaskStatusTest`, `TaskPriorityTest` | 创建、筛选、状态迁移、优先级 |
| 日程 | `ScheduleService`, `ScheduleItem`, `ScheduleConflictPolicy`, `ScheduleStatus`, `ScheduleQuery` | `ScheduleServiceTest`, `ScheduleItemTest`, `ScheduleConflictPolicyTest`, `ScheduleStatusTest`, `ScheduleQueryTest` | 时间范围、冲突、日期查询、动态状态 |
| 学习计划 | `StudyPlanService`, `StudyPlan`, `StudyPlanAnalysisService`, `StudyPlanStatus`, `StudyPlanQuery` | `StudyPlanServiceTest`, `StudyPlanTest`, `StudyPlanAnalysisServiceTest`, `StudyPlanStatusTest`, `StudyPlanQueryTest` | 进度边界、日期范围、状态和统计 |
| 收支 | `FinanceService`, `FinanceStatisticsService`, `TransactionRecord`, `TransactionQuery`, `TransactionType` | `FinanceServiceTest`, `FinanceStatisticsServiceTest`, `TransactionRecordTest`, `TransactionQueryTest`, `TransactionTypeTest` | 金额、类别、日期筛选、统计 |
| 笔记 | `NoteService`, `Note`, `NoteQuery`, `NoteSearchPolicy`, `Tag` | `NoteServiceTest`, `NoteTest`, `NoteQueryTest`, `NoteSearchPolicyTest`, `TagTest` | 标题内容校验、关键字、标签 |
| 汇总 | `SummaryService`, `DashboardSummary`, `LocalContext` | `SummaryServiceTest`, `DashboardSummaryTest`, `LocalContextTest` | 多模块汇总、本周本月边界、逾期任务、未来高优先级任务、AI 本地上下文 |
| 文档与 CI | `docs`, `.github/workflows/ci.yml` | `DocumentationDeliveryTest`, `CiWorkflowDeliveryTest` | 文档交付和 CI 命令存在性 |

## 附录 D：参考文档清单

| 文档 | 用途 |
|------|------|
| `/root/exp_SWAT/docs/1 requirement.md` | 原始需求和 8 个核心功能 |
| `/root/exp_SWAT/docs/2 design-oo.md` | OO 设计和模块职责 |
| `/root/exp_SWAT/docs/3 acceptance-and-next-iteration.md` | 验收清单和后续边界 |
| `/root/exp_SWAT/java-ai-assistant/README.md` | 项目运行、配置、测试和限制 |
| `/root/exp_SWAT/java-ai-assistant/docs/test-plan.md` | 测试计划 |
| `/root/exp_SWAT/java-ai-assistant/docs/test-cases.md` | 白盒测试用例 |
| `/root/exp_SWAT/acceptance/20260613_full_acceptance.md` | 2026-06-13 早期验收记录 |
| `/root/exp_SWAT/implements/202606131017_qa-risk-fixes/verify_v4.md` | QA 风险修复最新验证记录 |
| `/root/exp_SWAT/实验说明.md` | 提交材料和命名要求 |
| `/root/exp_SWAT/实验报告模板.md` | 实验报告结构与评分参考 |
